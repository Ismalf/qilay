module.exports = {
    darkMode: 'class', // Enable manual dark mode control
    theme: {
        extend: {
            // You can add custom colors or other settings here if needed
        },
    },
    plugins: [],
}